import { Box, Heading, Text } from '@chakra-ui/react';

const Sports = () => (
  <Box p={4}>
    <Heading mb={4}>Sports News</Heading>
    <Text fontSize="lg">Catch up on the latest sports events and scores.</Text>
  </Box>
);

export default Sports;